workflows:
  build-all-5-apps:
    name: Build All 5 Bharamputra Apps
    max_build_duration: 60
    instance_type: mac_mini_m1
    environment:
      groups:
        - android-signing
      vars:
        PACKAGE_NAME: "com.example.digitalindia"
      node: latest
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: 'main'
    scripts:
      - name: Set up local.properties
        script: |
          echo "sdk.dir=$ANDROID_SDK_ROOT" > local.properties
      - name: Force Gradle Wrapper Update
        script: |
          # Codemagic ke system gradle ko use karke naya wrapper banate hain
          gradle wrapper --gradle-version 9.3.1
          chmod +x gradlew
      - name: Build All 5 APKs
        script: |
          ./gradlew assembleMainAppRelease \
                    assembleMusicAppRelease \
                    assembleChatAppRelease \
                    assembleAdminAppRelease \
                    assembleAnalyticsAppRelease
    artifacts:
      - app/build/outputs/apk/*/*/*.apk
    publishing:
      email:
        recipients:
          - jaatsab123df@gmail.com
        notify:
          success: true
          failure: true
