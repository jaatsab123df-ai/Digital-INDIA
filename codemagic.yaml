workflows:
  build-all-5-apps:
    name: Build All 5 Bharamputra Apps
    max_build_duration: 60
    instance_type: mac_mini_m1
    environment:
      groups:
        - android-signing
      vars:
        PACKAGE_NAME: "com.example.digitalindia"
      node: latest
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: 'main'
    scripts:
      - name: Set up local.properties
        script: |
          echo "sdk.dir=$ANDROID_SDK_ROOT" > local.properties
      - name: Build All 5 APKs (Flavors)
        script: |
          # Use specifically version 9.3.1 as required by your project
          ./gradlew wrapper --gradle-version 9.3.1
          chmod +x gradlew
          ./gradlew assembleMainAppRelease \
                    assembleMusicAppRelease \
                    assembleChatAppRelease \
                    assembleAdminAppRelease \
                    assembleAnalyticsAppRelease
    artifacts:
      - app/build/outputs/apk/*/*/*.apk
    publishing:
      email:
        recipients:
          - jaatsab123df@gmail.com
        notify:
          success: true
          failure: true
